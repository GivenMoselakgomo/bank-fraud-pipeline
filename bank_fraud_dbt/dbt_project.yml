
# Project configuration file for dbt
name: 'bank_fraud_dbt'
version: '1.0.0'

profile: 'bank_fraud_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:        
  - "target"
  - "dbt_packages"


models:
  bank_fraud_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table
